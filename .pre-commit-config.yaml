repos:
  - repo: local
    hooks:
      - id: check
        name: Check packbits compression/decompression
        entry: deno test
        language: system
        files: mod(.\test)?\.ts$
        pass_filenames: false
